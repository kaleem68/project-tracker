query CountArchiveProjects{
    db_aggregateProject(
        where: {
            archived:{
                equals: true
            }
        }
    ){
        _count {
            id
        }
    }
}
