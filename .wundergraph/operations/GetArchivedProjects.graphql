query GetArchivedProject{
    db_findManyProject(where: {archived: {equals: true}}) {
        id,
        name,
        description,
        createdAt,
        budget,
        status
        archived
    }
}