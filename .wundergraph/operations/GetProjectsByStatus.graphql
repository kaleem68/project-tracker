query GetProjectsByStatus(
    $status: db_EnumStatusFilter!
){
    db_findManyProject(where: {status: $status}) {
        id,
        name,
        description,
        createdAt,
        budget,
        archived
    }
}